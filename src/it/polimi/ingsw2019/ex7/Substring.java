package it.polimi.ingsw2019.ex7;

public class Substring {


    /*
        assignable /nothing
        requires text != null && word != null && text.length >= word.length
        ensures |result <==> (\exists int i; 0 <= i < text.length; (\forall int j;
        0 <= j <= word.length; text[i+j] == word[j]))
     */

    public static boolean substring(char[] text, char[] word){
        return false;
    }
}
